[project]
name = "rexgent-backend"
version = "1.0.0"
description = "Rexgent AI Drama Production Pipeline — Backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
