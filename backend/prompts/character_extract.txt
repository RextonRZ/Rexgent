You are a casting director and character analyst. Extract complete character profiles from screenplays. Be thorough — include every NAMED character, even minor ones.

Extract all characters from this screenplay. Return a JSON array.

ONLY extract characters with a real PROPER NAME (a given name or full name). Do NOT extract a generic or placeholder figure as a character: never output a name like "UNKNOWN FIGURE", "Man", "Woman", "The Stranger", "Guard", "their brother", "the mother", "Villager", or "Figure 2". Those are extras — they belong in the action, not the cast. If the script refers to a person only by a role, a relationship, or "unknown", leave them OUT of this list entirely.

A NAMED ANIMAL or creature that appears ON SCREEN in stage directions across the story (a pet the plot follows, e.g. a rabbit named Snowy) IS a cast member: extract it with its species and coloring in physical_description ("a small white rabbit") and gender null-tolerant rules relaxed (use the script's pronouns or "male"). It needs a visual identity reference exactly like a human lead — leaving it out makes it render as a different animal in every shot. An animal that is only MENTIONED in dialogue and never shown stays out.

JSON schema for each character:
{
  "name": "string — full name as it appears in script",
  "role": "PROTAGONIST|ANTAGONIST|SUPPORTING|MINOR",
  "first_appearance_scene": 1,
  "gender": "male|female — REQUIRED, never null. Scripts rarely state it outright: infer it from the name, honorifics (MR./MRS./MS.), pronouns (he/she) and context. Every character gets cast a voice and a face, so commit to the most plausible reading even for minor roles.",
  "estimated_age": "string (e.g. 'late 30s', '20s', 'elderly') or null",
  "physical_description": "string — any physical details mentioned in script, or null",
  "personality_summary": "string — inferred from dialogue and actions, 2-3 sentences",
  "key_dialogue_samples": ["string — up to 3 memorable lines"],
  "speech_pattern": "string — formal|casual|terse|verbose",
  "emotional_arc": {
    "start": "string — character's emotional state at beginning",
    "midpoint": "string — state at story midpoint",
    "end": "string — state at conclusion"
  },
  "relationships": ["string — brief description of key relationships"]
}

Return ONLY the JSON array.

SCREENPLAY:
